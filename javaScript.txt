자바스크립트  
        //변수 선언
        //기존에 쓰던 변수 선언 문법
        var a; 
        
        
        //추천하는 방식
        const b = 10; //변하지 않는 값을 저장할 때는 const 사용
        // b = 20; 10을 20으로 바꿔서 오류 발생
        let c = 10; //변하는 값을 저장할 때는 let 사용
        c = 20;